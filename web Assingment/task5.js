// Assuming some code logic before reaching this point with a loop
// Let's say `i` is the index you're iterating through

if (/* some condition */) {
    // Code block if the condition is met
    // ...
  } else {
    console.log('---No match found at', i);
  }
  